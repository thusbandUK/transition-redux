import formatSubSuperScript from "./formatSubSuperScript";

const equationGenerator = (string) => {
    
    let arrayOfTaggedObjects = formatSubSuperScript(string);

    arrayOfTaggedObjects.forEach((entry) => {
        if (entry.)

    })

    //const outputArray = array.filter((entry) => {
        //return entry.childProps.true.includes(examBoard);
   // })
    //return outputArray;
}

export default equationGenerator;